services:
  - type: web
    name: quiz-telegram-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn main:server --bind 0.0.0.0:$PORT --workers 1 --threads 4 --timeout 0
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
